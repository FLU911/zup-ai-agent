# Словари действий по операциям руководства ЗУП

99 черновиков, сгенерированы 28.08.2026 автоматически из исходников
конфигурации ЗУП КОРП 3.1, которые лежат в `исходники-конфигурации`.

## Что это

По одному файлу на документ. Каждый файл — перечень того, что можно
сделать с объектом: закладки, поля шапки, флаги, переключатели, команды
формы, таблицы с колонками и их командами. Имена элементов настоящие,
взяты из `Documents\<Имя>\Forms\ФормаДокумента\Ext\Form.xml`.

На всех стоит `@каталог`, `@НеЗапускать`, `@черновик`.

## Чем это НЕ является

**Видимость и доступность элементов не проверены.** Form.xml знает
имена, но не знает, что покажет форма в конкретный момент: половина
элементов скрыта функциональными опциями, настройками учёта и
состоянием документа. Это выяснится только прогоном.

Именно поэтому стоит тег `@черновик`, а файлы лежат в `drafts/`, а не
в `.claude/skills/`. Черновик становится словарём после того, как
человек прогнал операцию на живой форме и вычистил лишнее.

Практический опыт этой сессии: на форме сотрудника поле номера лицевого
счёта присутствует в исходниках, но на живой форме **всегда невидимо и
недоступно** — правится только через регистр. Из Form.xml этого не
видно никак.

## Порядок доведения черновика до словаря

1. Прогнать операцию руками или через MCP, отмечая, что реально видно.
2. Выкинуть невидимое и неиспользуемое — файл должен быть коротким.
3. Сгруппировать по смыслу, а не по типу элемента: не «все поля шапки»,
   а «Заполнить шапку», «Указать сроки», «Настроить округление».
4. Дописать комментарии-ловушки. Они и есть главная ценность словаря:
   что называется обманчиво похоже, что срабатывает вхолостую, где
   заголовок расходится с именем.
5. Заменить `$$Значение$$` на осмысленные имена переменных.
6. Перенести в `.claude/skills/<скилл>/`.

## Покрытие по главам руководства

| Глава | Операций |
|---|---|
| 4. Ведение штатного расписания | 4 |
| 5. Учет кадров и ФОТ | 15 |
| 6. Ведение воинского учета | 2 |
| 7. Назначение удержаний | 6 |
| 8. Займы сотрудникам | 4 |
| 9. Отпуска, командировки, отсутствия | 12 |
| 10. Больничные и пособия ФСС | 4 |
| 11. Учет времени | 6 |
| 12. Межрасчетные начисления | 6 |
| 13. Натуральные и незарплатные доходы | 5 |
| 14. Ввод данных для расчета | 1 |
| 15. Расчет зарплаты и взносов | 3 |
| 16. Выплата зарплаты | 6 |
| 17. Обмен с банками | 5 |
| 18. НДФЛ и отчетность в ФНС | 8 |
| 19. Страховые взносы | 3 |
| 20. Данные для бухучета | 5 |
| 21. Исправление прошлых периодов | 4 |

Главы 1–3, 22 и приложения операций-документов не содержат: это
настройка программы, описание структуры предприятия и отчётность.

## Чего здесь нет

- **Отчётов.** Руководство описывает их много, но отчёт — не документ,
  у него другая механика (компоновка, настройки, варианты). Отдельная
  задача.
- **Справочников.** Генератор умеет их брать, но в план не включены:
  операции агента — это документы.
- **Уже готовых операций.** `НачислениеЗарплаты`,
  `НачислениеЗаПервуюПоловинуМесяца` и четыре ведомости не
  переписывались — по ним словари уже есть в `.claude/skills/`,
  и они проверены на живой форме.

## Генератор

`gen_slovar.py` + `plan.json` лежат в scratchpad сессии. Если понадобится
догенерировать — допишите документ в `plan.json` и запустите заново.
Скрипт сам находит форму: предпочитает `ФормаДокумента`, иначе
`ФормаЭлемента`, иначе `ФормаСписка`.

---

## Словари, снятые с ЖИВЫХ форм (не черновики)

Эти файлы прошли шаг 1–4 из порядка выше: операция прогнана через MCP,
имена проверены на форме, ловушки записаны. Тег `@черновик` с них снят.

| Файл | Чем ценен |
|---|---|
| `ВедомостьНаВыплатуЗарплаты.feature` | полный список видов выплат; пустой состав; ТЧ «НДФЛ» замораживает дату получения дохода; аванс не перезаполняется |
| `ПравкаСуммВПроведенныхДокументах.feature` | приём правки сумм; НДФЛ в начислении зарплаты не правится |
| `КвартальнаяОтчетность6НДФЛиЕФС1.feature` | подчёркивание в имени `РегламентированныйОтчет6_НДФЛ`; период только кнопками |
| `ПроверкаОтчетностиКСиВыгрузка.feature` | как читать результат КС и выгрузки; откуда берутся строки Раздела 2; разбор КС 1.11 / 1.14 / 1.45 |
| `ПереносДанныхПравкаРегистров.feature` | прямая запись в 66 регистров и чем она опасна |
| `УниверсальныйОтчетКакОкноВРегистры.feature` | **чтение любого регистра построчно** и перечисление метаданных |
| `ПризПодарок.feature` | дата получения дохода и как она ломает 6-НДФЛ |
| `ОтчетыПроверкиМесяца.feature` | чем проверять закрытый месяц; три сводных отчёта для заказчика; у каждой организации своя шапка колонок |
| `ЭлектроннаяПочтаПисьма.feature` | входящие и исходящие письма: как прочитать письмо не открывая форму, многострочный текст ответа, вложения через диалог выбора файла, отправка в два шага |
| `СотрудникиЛичныеДанные.feature` | завести человека по сканам: ФИО одной строкой, паспорт и место рождения в двух вложенных формах; формат хранения места рождения; чем опасен `set_value` без `exact_match` |
| `УдалениеОбъектов.feature` | пометка на удаление и обработка удаления помеченных: почему сотрудника не пометить из карточки, как узнать мешающую ссылку, регистры охраны труда от приёма, возврат писем из IMAP |

## Частично проверенные черновики

Файлы остались с тегом `@черновик` — прогнан только основной путь
операции, остальное по-прежнему снято из Form.xml. Проверенное вынесено
в конец файла отдельным блоком «ПРОВЕРЕНО НА ЖИВОЙ ФОРМЕ».

| Файл | Что прогнано 30.08.2026 |
|---|---|
| `ПриемНаРаботу.feature` | приём по позиции ШР: две кнопки «Провести» и работает не первая; позиция не выбирается вводом по строке; трудовая функция обязательна, а справочник пуст |
| `ИзменениеШтатногоРасписания.feature` | увеличение количества ставок: выбор из формы списка молча не срабатывает при выделении всех строк; ставки правятся только в форме позиции |
| `Увольнение.feature` | увольнение с компенсацией отпуска: доп. отпуска лежат в отдельной ТЧ; окно-предупреждение об увольнении ответственного лица; какие документы полагаются при увольнении и почему СТД-Р не проводится |

## Каталог метаданных

Отдельно от словарей лежит `drafts/метаданные/` — перечни имён
объектов конфигурации (документы, справочники, перечисления, регистры
накопления, начисления и удержания). Словарь отвечает «как работать
с объектом», каталог — «какие объекты есть и как называются».
