# Черновики и рабочие материалы

Здесь то, что ещё не стало скиллом или словарём: рабочие записи по базе,
отчёты о прогонах моделей, метаданные конфигурации и инструменты.

Словари действий переехали в `словари/` — там 112 файлов, по одному
на документ. Три скилла, которые раньше лежали здесь черновиками
(`закрытие месяца`, `квартальная отчётность`, `место выплаты`),
перенесены в `.claude/skills/` 31.08.2026.

Старые исполняемые сценарии `ЕФС1.feature` и `Расчет_отпуска.feature`
удалены в тот же день: всё, что в них было ценного — имена элементов —
уже перенесено в словари `КвартальнаяОтчетность6НДФЛиЕФС1.feature`
и `Отпуск.feature`, а сами файлы были написаны по старой схеме,
с `Контекст:` и запуском целиком. Найти их можно в истории git.

## Что здесь осталось

| Папка / файл | Что это |
|---|---|
| `словари/` | 112 словарей действий по документам |
| `метаданные/` | каталог объектов конфигурации, снятый без конфигуратора |
| `ДЛЯ-ДРУГИХ-ИИ/` | комплект для запуска других моделей на этих же задачах |
| `ОТЧЕТ-*.md` | отчёты о прогонах DeepSeek, Qwen и связки Opus + DeepSeek |
| `РЕЦЕПТ-закрытие-месяца-по-шагам.md` | пошаговый разбор месяца с ловушками |
| `инструменты/` | конвертеры выгруженных табличных документов |

## Инструменты

`инструменты/xlsx2tsv.py` — конвертер выгруженных табличных документов
1С в TSV **с сохранением номеров колонок**. Нужен там, где в отчёте
есть пустые ячейки: старый `xlsx2txt.py` их выбрасывает, колонки
съезжают, и разбор по индексу даёт правдоподобный мусор.

```
python инструменты/xlsx2tsv.py отчет.xlsx > отчет.tsv
```

Под Windows запускать с `PYTHONIOENCODING=utf-8`.

## Каталог метаданных

`метаданные/` — перечни имён объектов конфигурации, снятые с живой
базы без конфигуратора. См. `метаданные/README.md`.

## Задания для проверки других моделей

`ДЛЯ-ДРУГИХ-ИИ/06-БЕНЧМАРК-ЗАДАНИЯ.md` — 20 заданий пяти уровней
сложности с объективными критериями приёмки и бланком протокола.

## Черновики скиллов

Запись в `.claude/skills/**` запрещена настройками проекта, поэтому
новые скиллы копятся здесь. Переносит человек.

| Файл | Про что |
|---|---|
| `SKILL-закрытие-месяца.md` | конвейер месяца: что за чем и что нельзя раньше чего |
| `SKILL-место-выплаты.md` | каналы выплаты по организациям |
| `SKILL-квартальная-отчетность.md` | 6-НДФЛ, ЕФС-1, персонифицированные: что заполнить до создания отчёта, как читать КС и что делать, когда они не сходятся |
