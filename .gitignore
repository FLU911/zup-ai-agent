# ── чужие материалы, которые нельзя публиковать ──────────────────────
# исходники конфигурации 1С:ЗУП и методическая документация «1С»:
# папки нужны агенту, но их содержимое в репозиторий не попадает
исходники-конфигурации/*
!исходники-конфигурации/README.md
документация-зуп/*
!документация-зуп/README.md

# ── синтетические сканы документов ───────────────────────────────────
# генератор в репозитории есть, картинки — нет: их размер ни к чему,
# и любой желающий получает свой комплект одной командой
#   python authoring/scans/generate_scans.py --count 20
authoring/scans/комплекты/
authoring/scans/эталон/

# ── результаты работы агента ─────────────────────────────────────────
out/
scratchpad/
drafts/почта-агент/tmp/
drafts/tmp/
почта/tmp/
календарь/tmp/
*.xlsx
*.mxl
*.pdf
*.eml
*.xml

# ── локальные настройки и секреты ────────────────────────────────────
.claude/settings.local.json
почта/настройки.json
календарь/настройки.json
*.local.json

# ── 1С ───────────────────────────────────────────────────────────────
*.dt
*.cf
*.epf
*.log

# ── python ──────────────────────────────────────────────────────────
__pycache__/

# ── задачник «1С:Спеццон ЗУП 2025» ───────────────────────────────────
# платный сборник: тексты заданий и их пересказ в публичный репозиторий
# не выкладываем. Журнал прогона и скрипт отметок — свои, остаются
drafts/задачник/*
!drafts/задачник/ЖУРНАЛ.md
!drafts/задачник/отметить.py
