---
description: Переделать исполняемый сценарий Vanessa в словарь действий
---

Прочитай целиком `.claude/skills/feature-to-dictionary/SKILL.md` и все файлы, на которые
он ссылается, и работай строго по нему.

Правила проекта — в `CLAUDE.md`, они обязательны: имена элементов формы брать
только из словарей, после каждого ввода проверять значение, feature-файлы
не редактировать.

Спроси, какой сценарий переделывать, если человек его не назвал. Ключ `--прогнан`
ставится только тогда, когда в `authoring/ТестПлан.MD` написано, что сценарий
прошёл на живой базе: непроверенный словарь в `drafts/словари/` не кладут.

$ARGUMENTS
