---
description: Расчётный день: наступил ли день аванса или зарплаты — начислить и выплатить
---

Прочитай целиком `.claude/skills/zup-payday-watch/SKILL.md` и все файлы, на которые
он ссылается, и работай строго по нему.

Правила проекта — в `CLAUDE.md`, они обязательны: имена элементов формы брать
только из словарей, после каждого ввода проверять значение, feature-файлы
не редактировать.

Выполни ОДИН тик расчётного цикла. Сегодня обычный день и догонять нечего —
так и скажи одной строкой.

$ARGUMENTS
