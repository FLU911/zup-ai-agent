---
description: Отчётный день: подошёл ли срок сдачи — сформировать и проверить отчётность
---

Прочитай целиком `.claude/skills/zup-reporting-watch/SKILL.md` и все файлы, на которые
он ссылается, и работай строго по нему.

Правила проекта — в `CLAUDE.md`, они обязательны: имена элементов формы брать
только из словарей, после каждого ввода проверять значение, feature-файлы
не редактировать.

Выполни ОДИН тик отчётного цикла. Отчётность только формируй и проверяй —
отправляет её человек. Сроков нет и вчерашних кадровых событий нет — так и
скажи одной строкой.

$ARGUMENTS
