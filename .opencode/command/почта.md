---
description: Проверить входящую почту 1С и выполнить задания из писем
---

Прочитай целиком `.claude/skills/zup-mail-watch/SKILL.md` и все файлы, на которые
он ссылается, и работай строго по нему.

Правила проекта — в `CLAUDE.md`, они обязательны: имена элементов формы брать
только из словарей, после каждого ввода проверять значение, feature-файлы
не редактировать.

Выполни ОДИН тик почтового цикла. Если в конце ничего нового не нашлось — так и скажи одной строкой.

$ARGUMENTS
