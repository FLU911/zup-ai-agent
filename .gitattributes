# Порядок важен: побеждает ПОСЛЕДНЕЕ совпавшее правило.
# Поэтому общее правило идёт первым, а исключение для feature — после него.
* text=auto

# feature-файлы Vanessa Automation — UTF-8 with BOM.
# Никаких преобразований: клонируем байт в байт.
*.feature -text
